using UnityEngine;

public class PlayerManager : MonoBehaviour
{
    //Possibly don't need this class, but just in case we need to manage player related things like stats, inventory, etc. we can put it here. Otherwise, we can just use the InventoryManager and GameDataManager for that stuff and get rid of this class entirely.
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
